
# AI assistant artifacts
context-snapshot.json
state.md
session-log.md

# OS / local tooling
.DS_Store
# .claude lokal ignorieren, ABER die Projekt-Skills versionieren —
# sie sind Teil des Arbeits-Workflows (laden in jeder Session, für alle).
.claude/*
!.claude/skills/
.superpowers/
